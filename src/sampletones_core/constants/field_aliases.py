from typing import Dict, Final

ALIASES: Final[Dict[str, str]] = {
    "sr": "sample_rate",
    "nf": "nes_frequency",
    "ws": "window_size",
    "tg": "transformation_gamma",
    "gn": "generators",
    "ch": "config_hash",
}
