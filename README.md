# SampleTONes

v0.2.0


## Installation

### Requirements
- Python 3.13 (https://www.python.org/downloads/)
- Windows, macOS, or Linux

### Windows
1. Make sure Python 3.13 is installed and available as `python` in your PATH.
2. Double-click `install.bat` in this folder. It will build a standalone `sampletones.exe` in the same directory.
3. Run `sampletones.exe` to start the application.

### macOS / Linux
1. Make sure Python 3.13 is installed and available as `python3` in your PATH.
2. Open a terminal in this folder and run:
	```sh
	chmod +x install.sh
	./install.sh
	```
	This will build a standalone `sampletones` executable in the same directory.
3. Run `./sampletones` to start the application.

### Uninstall / Cleanup
Just delete the folder to remove everything. No system Python packages are affected.

---
Add description, usage, and examples below.
